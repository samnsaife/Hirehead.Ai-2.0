1. Set environment variable BACKEND_URL to your deployed backend URL.
2. On Render create a Web Service for frontend (Node). Build and Start commands:
- Build Command: `npm install && npm run build`
- Start Command: `npm start`
3. Alternatively run locally with `npm install && npm run dev`.
